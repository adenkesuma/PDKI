import { NewsProps } from "@/utils/interface"

const CardNews = ({
    title,
    content,
    description,
    image,
    published,
    publishedDate,
    categories,
    region,
    video,
    tags
} : NewsProps) => {
  return (
    <div className="">
        card news
    </div>
  )
}

export default CardNews